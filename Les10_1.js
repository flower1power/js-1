//!Функции высшего порядка

/* 
  ФУНКЦИЯ ПЕРВОГО КЛАССА
  В JS функции это просто значения
    const a = (b) => b++; Хранение переменной

    function g(a) { Передача в другие функции
      return a; Возвращение из функции
    }
*/


/* 
  ФУНКЦИЯ ВЫСШЕГО ПОРЯДКА
  В JS функции принимают или / и возвращают другие функции
    function g(a){ 

    }Принимает другие функции

    function f() { 
      return a;
    }Возвращает другие функции
*/